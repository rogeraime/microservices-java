spring:
  application:
    name: authservice
  cloud:
      discovery:
        healthCheckPath: /monitor/health
        healthCheckInterval: 15s
      config:
        enabled: true
        format: YAML
        prefix: config
management:
  context-path: /monitor
  